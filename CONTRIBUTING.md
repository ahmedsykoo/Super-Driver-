# المساهمة في Super Driver

قبل أي تعديل:

1. راجع مسارات الحزم واسم التطبيق و`applicationId`.
2. شغّل الاختبارات المحلية الممكنة.
3. شغّل `testDebugUnitTest` و`lintDebug` عبر Gradle.
4. لا تضف ملفات Kotlin أو Android إلى `.github/workflows`.
5. لا تضع مشروع Android داخل ZIP آخر داخل المستودع.
6. لا تضف أي أتمتة لقبول أو رفض الرحلات.
